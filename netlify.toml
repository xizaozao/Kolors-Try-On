[build]
  # 你的构建命令（根据项目实际情况修改，无构建可留空或写自定义命令）
  command = "pip install -r requirements.txt"
  # 构建输出目录（静态页/文档类项目填对应目录，如 docs/_site，无构建可填当前目录 .）
  publish = "."

[build.environment]
  # 核心：指定 Python 版本，推荐 3.10
  PYTHON_VERSION = "3.10.14"
